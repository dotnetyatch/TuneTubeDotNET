@{
    ViewData["Title"] = "Pictures, Movies Album Arts";
    @model IEnumerable<QueryResult>
}

<div class="row" id="search">

    <div class="col-md-8 col-md-offset-2">
        <form>
            <div class="form-group">
                <label>Enter Picture or Video title:</label>
                <input type="text" name="title" class="form-control" />
            </div>
            <div class="form-group">

                <input type="submit" name="Add" class="btn btn-success" />
            </div>

        </form>
    </div>
    </div>
    <div class="col-md-8 col-md-offset-2">
    <h2></h2>
    <ul>
        @foreach (var item in Model)
        {
        <li class="picitem"> <img src=" @Html.DisplayFor(modelItem => item.url)" width="200" height="200"/> </li>
        }

    </ul>
    </div>
